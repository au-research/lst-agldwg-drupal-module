langcode: en
status: true
dependencies:
  module:
    - menu_ui
  enforced:
    module:
      - x1
third_party_settings:
  menu_ui:
    available_menus:
      - main
    parent: 'main:'
name: Register
type: register
description: "A Register, in the context of registering IRIs with the AGLDWG, is a sub-register of the domain linked.data.gov.au. A \"system\" register exists at https://linked.data.gov.au/reg/ - the Register that lists all other top-level Registers. Then there are Registers per Registered Item type: /def/, /dataset/ & /org/.\r\n\r\nNew Registers may be registered by the AGLDWG but are not expected to be requested by individual AGLDWG members directly."
help: ''
new_revision: true
preview_mode: 1
display_submitted: true
