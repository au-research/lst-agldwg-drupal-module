langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_publisher
    - node.type.organisation
    - node.type.register
id: node.register.field_publisher
field_name: field_publisher
entity_type: node
bundle: register
label: Publisher
description: "The Organisation that publishes this Resource. This field is mapped to dcterms:publisher.\r\n\r\nThe creator might not be the same as the Organisation applying for the URI or the Creator.\r\n\r\nIf the organisation you wish to link to doesn't appear in the list, you will need to ask the AGLDWG co-chairs to register that organisation for you first."
required: true
translatable: true
default_value: {}
default_value_callback: ''
settings:
  handler: 'default:node'
  handler_settings:
    target_bundles:
      organisation: organisation
    sort:
      field: title
      direction: ASC
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
